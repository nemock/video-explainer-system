# Local Claude Code session / scheduled-task state
.claude/

# Python
.venv/
__pycache__/
*.pyc
.uv/

# Node / Playwright
node_modules/

# Local model caches / downloads
models/
.cache/

# Per-run generated artifacts (heavy; not source)
outputs/
frames/

# Atomized knowledge wiki — operator-specific generated content; not source code
wiki/

# Phase 0 spike: generated artifacts (keep the scripts + README, not the outputs)
phase0/work/
phase0/video/

# OS
.DS_Store

# Brand/logo assets must NOT live in this dev repo (use ~/.claude/explainer-brands/)
BRG Logos/
*.png.import

# Deep-dive operator content (brand assets, licensed music, per-program builds) —
# this repo tracks only the deep-dive design docs + future code. (deep-dive/wiki/ is
# already covered by the wiki/ rule above.)
deep-dive/brand/
deep-dive/shared/
deep-dive/programs/
